{
  "name": "express-weather",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "devDependencies": {
    "axios": "^0.25.0",
    "dotenv": "^15.0.0",
    "express": "^4.17.2"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm i && npm run build"
  }
}
